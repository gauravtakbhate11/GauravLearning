const arr = ["brad", "sara", "sam", "john", "tim"];

for (let i = 0; i < arr.length; i++) {
  if (arr[i] === "john") {
    console.log(arr[i] + " is the best");
  } else {
    console.log(arr[i]);
  }
}
